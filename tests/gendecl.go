package tests

import "fmt"

const GC = "gendecl"

const (
	GCA      = "GCA"
	gcb      = "gcb"
	GCC, gcd = "GCC", "gcd"
)

var GV = fmt.Sprintf("gendecl")

var (
	GVA = "GVA"
	gvb = "gvb"
)

type GSE struct{}

type gsu struct{}
